# Database Configuration
# Note: The server currently uses dbConfig.json for database connection
# This .env file is for environment variables

# Server Configuration
PORT=4000

# JWT Secret (for authentication)
SECRET_KEY=your-secret-key-here

# MongoDB Configuration (if using environment variables instead of dbConfig.json)
# MONGODB_CONNECTION_URI=your-mongodb-connection-string
# MONGODB_DATABASE_NAME=ChurchDB
